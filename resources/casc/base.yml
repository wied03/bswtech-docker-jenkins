jenkins:
  systemMessage: From the plugin!
  clouds:
  - computeEngine:
      cloudName: "ourslave"
      configurations:
      - description: "Jenkins slaves"
        labels: "docker.build ruby.build"
        launchTimeoutSeconds: 300
        namePrefix: "jenkins-slave"
        numExecutors: 2
        region: "https://www.googleapis.com/compute/v1/projects/bsw-tech/regions/us-west1"
        retentionTimeMinutes: 120
        runAsUser: "jenkins"
        template: "https://www.googleapis.com/compute/v1/projects/bsw-tech/global/instanceTemplates/jenkins-slave-20191231203812125200000001"
        zone: "https://www.googleapis.com/compute/v1/projects/bsw-tech/zones/us-west1-a"
      credentialsId: "bsw-tech"
      instanceCapStr: "2"
      projectId: "bsw-tech"
credentials:
  system:
    domainCredentials:
    - credentials:
      - googleRobotPrivateKey:
          projectId: "bsw-tech"
          serviceAccountConfig:
            json:
              filename: stuff.json
              secretJsonKey: ${somesecret}
